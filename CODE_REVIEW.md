# CJQT6 代码审查报告（第五轮 - 全面复查）

> 第五轮审查日期：2026-03-23 12:06
> 审查范围：全部 src/ 模块代码

---

## 一、新发现的问题

### 🔴 严重问题：多个核心类仍存在终结器，可能导致崩溃

以下类有**启用的终结器**，与 `QTimer` 之前的问题相同：

| 类 | 文件 | 行号 |
|----|------|------|
| `QWidget` | `core/widget.cj` | 145 |
| `QLabel` | `widgets/label.cj` | 120 |
| `QLineEdit` | `widgets/lineedit.cj` | 335 |
| `QPushButton` | `widgets/pushbutton.cj` | 192 |
| `QSpinBox` | `widgets/spinbox.cj` | 139 |

**问题原因：** 仓颉的 GC 会在不确定的时机调用终结器。当这些对象被全局变量或其他长生命周期变量持有时，终结器可能在 Qt 事件循环仍在使用这些对象时被调用，导致 `qWidgetDelete` 等函数在 Qt 侧产生未定义行为。

**修复方案：** 参照 `QTimer` 的修复方式，注释掉所有这些类的终结器，并添加说明注释。

```csharp
// QWidget 修复示例 (widget.cj):
/// 终结器 - 已禁用
/// 原因：仓颉 GC 调用时机不确定，可能在 Qt 对象仍被使用时提前删除
// ~init() {
    // ...
// }
```

---

## 二、之前遗留问题的状态

| 问题 | 状态 |
|------|------|
| tank_battle 渲染循环每帧泄漏绘图对象 | ❌ 未修复 |
| GameRenderer.brush 覆盖泄漏 | ❌ 未修复 |
| QPainter 缺少 delete() 方法 | ❌ 未修复 |
| getDouble() 取消检测不一致 | ❌ 未修复 |

---

## 三、本轮复查结论

### 代码库整体质量

经过多轮修复，CJQT6 库的核心设计已经较为完善：

- ✅ FFI 声明规范
- ✅ 工厂方法无指针泄漏（已修复）
- ✅ 大部分类有 delete() 方法
- ✅ 对话框 API 设计合理
- ✅ 信号回调机制完整

### 关键风险

1. **终结器问题**（严重）- 5 个核心类需要立即禁用终结器
2. **tank_battle 渲染泄漏**（严重）- 影响长期运行稳定性

### 建议

1. **立即修复**：禁用 `QWidget`、`QLabel`、`QLineEdit`、`QPushButton`、`QSpinBox` 的终结器
2. **后续优化**：修复 tank_battle 的渲染内存泄漏

---

*本报告由 AI 代码审查工具生成*
